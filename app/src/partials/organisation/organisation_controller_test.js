'use strict';

describe('myApp.organisation module', function() {

  beforeEach(module('myApp.organisation'));

  describe('organisation controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var organisationCtrl = $controller('OrganisationCtrl');
      expect(organisationCtrl).toBeDefined();
    }));

  });
});