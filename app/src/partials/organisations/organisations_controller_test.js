'use strict';

describe('myApp.organisations module', function() {

  beforeEach(module('myApp.organisations'));

  describe('organisations controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var organisationsCtrl = $controller('OrganisationsCtrl');
      expect(organisationsCtrl).toBeDefined();
    }));

  });
});