'use strict';

describe('myApp.volunteer module', function() {

  beforeEach(module('myApp.volunteer'));

  describe('volunteer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var volunteerCtrl = $controller('VolunteerCtrl');
      expect(volunteerCtrl).toBeDefined();
    }));

  });
});